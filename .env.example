# Database
DATABASE_URL="postgresql://user:password@localhost:5432/razorrecover?schema=public"

# Auth.js
AUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Claude API (AI diagnosis + agent). Falls back to deterministic rules if unset.
CLAUDE_API_KEY=""

# Razorpay (Test Mode). Falls back to MockPaymentProvider if unset.
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
RAZORPAY_WEBHOOK_SECRET=""
